spring:
  jpa:
    show-sql: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

server:
  forward-headers-strategy: framework

app:
  security:
    cookie-secure: true

logging:
  level:
    root: WARN
    com.nfcplatform: INFO
